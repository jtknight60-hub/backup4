export const environment = {
  production: false,
  // In development we use the Angular dev proxy which forwards `/api` to the
  // backend (configured in `proxy.conf.json`). This avoids CORS and lets the
  // frontend call the backend via the same origin.
  apiUrl: '/api'
};